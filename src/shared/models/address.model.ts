export interface Address {
  street: string,
  number: number;
  complement: string;
  city: string;
  neighborhood: string;
  state: string;
  postalCode: string;
}

export interface FederativeUnit {
  name: string,
  abbreviation: string
}
